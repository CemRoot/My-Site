name: System Health Check

# Daily health check - runs once per day
on:
  schedule:
    # Run at 08:00 UTC every day (before first scraping)
    - cron: '0 8 * * *'
  
  # Manual trigger
  workflow_dispatch:

# Prevent multiple runs
concurrency:
  group: system-health-check
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  health-check:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      
      - name: 📦 Install dependencies
        run: npm ci
      
      # Covers every scope — scraper, LinkedIn digest and the live site chat — so a
      # retirement that would break any of them is reported here on a schedule,
      # not discovered when that pipeline next runs.
      - name: 🤖 Verify all Groq models are still available
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GROQ_PARSER_API_KEY: ${{ secrets.GROQ_PARSER_API_KEY }}
          # Model overrides — the health check must probe the same models the
          # pipelines actually call, otherwise setting a variable to route around
          # a retirement would leave this reporting on the old default.
          GROQ_PRIMARY_MODEL: ${{ vars.GROQ_PRIMARY_MODEL }}
          GROQ_FALLBACK_MODEL: ${{ vars.GROQ_FALLBACK_MODEL }}
          GROQ_LAST_RESORT_MODEL: ${{ vars.GROQ_LAST_RESORT_MODEL }}
          GROQ_ENHANCEMENT_MODEL: ${{ vars.GROQ_ENHANCEMENT_MODEL }}
          GROQ_FAST_MODEL: ${{ vars.GROQ_FAST_MODEL }}
          GROQ_PARSER_MODEL: ${{ vars.GROQ_PARSER_MODEL }}
          GROQ_LINKEDIN_MODEL: ${{ vars.GROQ_LINKEDIN_MODEL }}
          CHAT_GROQ_MODEL: ${{ vars.CHAT_GROQ_MODEL }}
          CHAT_GROQ_FALLBACK_MODEL: ${{ vars.CHAT_GROQ_FALLBACK_MODEL }}
        run: node scripts/ci/check-groq-models.mjs --scope=all

      - name: 🏥 Run system health check
        env:
          # Supabase
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          
          # APIs
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GROQ_PARSER_API_KEY: ${{ secrets.GROQ_PARSER_API_KEY }}
          FIRECRAWL_API_KEY: ${{ secrets.FIRECRAWL_API_KEY }}

          # Model overrides — the health check must probe the same models the
          # pipelines actually call, otherwise setting a variable to route around
          # a retirement would leave this reporting on the old default.
          GROQ_PRIMARY_MODEL: ${{ vars.GROQ_PRIMARY_MODEL }}
          GROQ_FALLBACK_MODEL: ${{ vars.GROQ_FALLBACK_MODEL }}
          GROQ_LAST_RESORT_MODEL: ${{ vars.GROQ_LAST_RESORT_MODEL }}
          GROQ_ENHANCEMENT_MODEL: ${{ vars.GROQ_ENHANCEMENT_MODEL }}
          GROQ_FAST_MODEL: ${{ vars.GROQ_FAST_MODEL }}
          GROQ_PARSER_MODEL: ${{ vars.GROQ_PARSER_MODEL }}
          GROQ_LINKEDIN_MODEL: ${{ vars.GROQ_LINKEDIN_MODEL }}
          CHAT_GROQ_MODEL: ${{ vars.CHAT_GROQ_MODEL }}
          CHAT_GROQ_FALLBACK_MODEL: ${{ vars.CHAT_GROQ_FALLBACK_MODEL }}
          
          # Telegram
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          echo "🏥 Running comprehensive system health check..."
          echo "📅 Date: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "🔍 Checking all system components..."
          echo ""
          npm run health:check
      
      - name: ✅ Health check passed
        if: success()
        run: |
          echo "✅ All systems healthy!"
          echo "📊 Health report sent to Telegram"
          echo "⏰ Completed at: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
      
      - name: ❌ Health check failed
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          echo "❌ System health check failed!"
          CURRENT_TIME=$(date -u +'%Y-%m-%d %H:%M:%S UTC')
          LOG_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          
          MESSAGE="🚨 <b>SİSTEM SAĞLIK KONTROLÜ - KRİTİK HATA</b>%0A%0A❌ Sistem sağlık kontrolü başarısız oldu%0A⏰ Zaman: ${CURRENT_TIME}%0A%0A🔍 <b>Kritik Sistem Hatası Tespit Edildi</b>%0AEn az bir sistem bileşeni düzgün çalışmıyor.%0A%0A📊 <b>Kontrol Edilmesi Gerekenler:</b>%0A• Supabase bağlantısı%0A• Firecrawl API durumu%0A• Groq AI API durumu%0A• Telegram Bot durumu%0A%0A🔗 <a href='${LOG_URL}'>Detaylı log kayıtlarını incele</a>%0A%0A<i>⚠️ Acil müdahale gerekebilir!</i>"
          
          curl -s --retry 4 --retry-delay 2 --retry-all-errors --max-time 30 -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${MESSAGE}" \
            -d "parse_mode=HTML" \
            -d "disable_web_page_preview=false" > /dev/null || true
          
          echo "📱 Critical error notification sent to Telegram"
          echo "Check the logs: ${LOG_URL}"

