name: Scrape Tech News

# Smart scheduling algorithm
# WEEKDAYS ONLY: Nuvemmag doesn't publish on weekends
# 
# TIMEZONE NOTE: Nuvemmag is a Turkish news site (Europe/Istanbul timezone)
# The script automatically handles timezone using JavaScript Intl API,
# including any DST (Daylight Saving Time) changes - no manual updates needed!
#
# Schedule is in UTC (GitHub Actions standard):
# - Morning, afternoon, and evening checks to catch articles throughout the day
# - Final check is before n8n LinkedIn digest workflow (16:30 UTC)
on:
  schedule:
    # Monday-Friday only - Three daily checks
    - cron: '0 7 * * 1-5'    # 07:00 UTC - Morning check (catches overnight articles)
    - cron: '0 12 * * 1-5'   # 12:00 UTC - Afternoon check (catches morning articles)
    - cron: '0 16 * * 1-5'   # 16:00 UTC - Final check (before n8n digest at 16:30 UTC)
  
  # Manual trigger
  workflow_dispatch:

# Prevent multiple runs
concurrency:
  group: tech-news-scraper
  cancel-in-progress: true

jobs:
  scrape-and-translate:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      
      - name: 📦 Install dependencies
        run: npm ci
      
      - name: 🔍 Scrape and translate tech news
        env:
          # GitHub Secrets - These will be available at runtime
          # IDE warnings are normal - secrets are not accessible locally
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          FIRECRAWL_API_KEY: ${{ secrets.FIRECRAWL_API_KEY }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # Set timezone for Node.js (used for date calculations)
          TZ: UTC
        run: |
          echo "🚀 Starting multi-category tech news scraper..."
          echo "📂 Categories: 7 (AI, Tech, Science, etc.)"
          echo "📊 Articles per category: 15 (Get all daily news)"
          echo ""
          echo "📅 Time Info:"
          echo "   UTC now: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "   Turkey now: $(TZ='Europe/Istanbul' date +'%Y-%m-%d %H:%M:%S %Z')"
          echo ""
          echo "🌍 Timezone: Script uses Europe/Istanbul (auto DST handling)"
          echo "📰 Filtering: Recent articles (last 3 days)"
          echo "💾 Storage: Supabase PostgreSQL"
          echo "🔄 Firecrawl Cache: maxAge=2h (fresh data for scheduled runs)"
          echo ""
          npm run scrape:news
      
      - name: ✅ Success summary
        if: success()
        run: |
          echo "🎉 Tech news successfully scraped and saved to Supabase!"
          echo "📊 Articles saved directly to PostgreSQL database"
          echo "⏰ Completed at: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "💾 Storage: Supabase (no git commit needed)"
          echo "🔄 Website will fetch latest data from database automatically"
      
      - name: 📱 Send success notification to Telegram
        if: success()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # Set to 'false' to disable success notifications (only get error alerts)
          NOTIFY_SUCCESS: 'true'
        run: |
          if [ "$NOTIFY_SUCCESS" = "true" ]; then
            CURRENT_TIME=$(date -u +'%Y-%m-%d %H:%M:%S UTC')
            
            # Check how many articles were added (parse from npm output if available)
            # For now, send a simple success notification
            MESSAGE="✅ <b>TECH NEWS SCRAPER - BAŞARILI</b>%0A%0A🎉 Haber toplama işlemi başarıyla tamamlandı!%0A⏰ Zaman: ${CURRENT_TIME}%0A💾 Veritabanı: Supabase PostgreSQL%0A🔄 Durum: Aktif%0A%0A<i>Tüm sistemler normal çalışıyor</i>"
            
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d "chat_id=${TELEGRAM_CHAT_ID}" \
              -d "text=${MESSAGE}" \
              -d "parse_mode=HTML" \
              -d "disable_web_page_preview=true" > /dev/null
            
            echo "📱 Telegram bildirim gönderildi"
          else
            echo "📱 Başarı bildirimi devre dışı (sadece hata bildirimleri aktif)"
          fi
      
      - name: 📧 Send notification on failure
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          echo "❌ Scraping failed!"
          CURRENT_TIME=$(date -u +'%Y-%m-%d %H:%M:%S UTC')
          LOG_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          
          MESSAGE="🚨 <b>TECH NEWS SCRAPER - HATA!</b>%0A%0A❌ Haber toplama işlemi başarısız oldu%0A⏰ Zaman: ${CURRENT_TIME}%0A🔧 Workflow: Scrape Tech News%0A📊 Durum: Başarısız%0A%0A🔍 <b>Olası Sorunlar:</b>%0A• API anahtarı hatası (Firecrawl/Groq)%0A• Veritabanı bağlantı sorunu (Supabase)%0A• Rate limit aşımı%0A• Kaynak site erişim sorunu%0A%0A🔗 <a href='${LOG_URL}'>Log kayıtlarını incele</a>%0A%0A<i>⚠️ Güvenlik uyarısı: Sistem otomatik olarak tekrar deneyecek</i>"
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${MESSAGE}" \
            -d "parse_mode=HTML" \
            -d "disable_web_page_preview=false" > /dev/null
          
          echo "📱 Hata bildirimi Telegram'a gönderildi"
          echo "Check the logs: ${LOG_URL}"

